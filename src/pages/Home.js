import React from "react";
import TodoPage from "../components/TodoPage";
import Layout from "../layouts/Layout";

const Home = () => {
  return (
    <Layout>
      <TodoPage />
    </Layout>
  );
};

export default Home;
